<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.falcon.shop.mapper.admin.BannerMapper">

    <select id="listWithParams" parameterType="com.falcon.shop.domain.common.QueryParams" resultType="Banners">
        SELECT *
        FROM banners
        WHERE 1=1
        <if test="queryParams.search != null and queryParams.search != ''">
            AND (
                name LIKE CONCAT('%', #{queryParams.search}, '%')
                OR type LIKE CONCAT('%', #{queryParams.search}, '%')
                OR url LIKE CONCAT('%', #{queryParams.search}, '%')
            )
        </if>
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY 
        <choose>
            <when test="sort != null and sort != ''">
                <choose>
                    <when test="sort == 'name'">name</when>
                    <when test="sort == 'type'">type</when>
                    <when test="sort == 'url'">url</when>
                    <when test="sort == 'createdAt'">created_at</when>
                    <otherwise>no</otherwise>
                </choose>
                <choose>
                    <when test="order != null and order == 'asc'">ASC</when>
                    <otherwise>DESC</otherwise>
                </choose>
            </when>
            <otherwise>no DESC, created_at DESC</otherwise>
        </choose>
    </select>

</mapper>
